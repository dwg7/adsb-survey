# adsb-survey — Claude Code 向けのプロジェクト文脈

新しいセッションを始めるときは、まずこのファイルを読むこと。

姉妹プロジェクト: [dwg7/kikicom](https://github.com/dwg7/kikicom)(札幌・月寒の RTL-SDR + readsb の
受信機1台でADS-Bを学ぶプロジェクト。このプロジェクトの着想はそこから生まれた)

---

## 1. 目的

**航空測量(写真測量・レーザ計測など)の飛行を、オープンな ADS-B データから技術的に読み解く。**
ユーザーの主要な関心は航空測量であり、このプロジェクトは**技術の観点**で行う(事業の観点ではない)。

扱いたい問いの例:

- 航空測量機はどういう飛び方をするか(撮影測線の間隔・向き・高度・速度、測線間の旋回)
- 航跡から「移動中」と「撮影中」を自動で見分けられるか
- ある日・ある地域で、撮影飛行がどの範囲をカバーしたか
- 災害の直後に、緊急の撮影飛行がどこに集まるか
- ADS-B の受信網の厚さ・欠けが、こうした解析をどこまで制約するか

きっかけ: 2026-09-22 朝、kikicom の受信機が丘珠を離陸する C208(JA13AJ、航空測量機)を捉え、
adsb.lol 上で同じ朝に淡路島付近を 82 ノットで飛ぶ C208(JA11AJ)も見えたこと。

---

## 2. データとライセンス

**データはリポジトリに入れない。** `data/` に置き、`.gitignore` 済み。
**解析結果もこのリポジトリからは公開しない**(ユーザー自身の学習用)。ツールだけを CC0 で出す。

| データ | 入手 | ライセンス・条件 |
|---|---|---|
| adsb.lol 日次アーカイブ | GitHub `adsblol/globe_history_YYYY` のリリース(1日1リリース、前日分が翌朝) | **ODbL** |
| adsb.lol API | `https://api.adsb.lol/v2/...`(`/type/{機種}`、`/hex/{ICAO}`、`/reg/{登録}`、`/point/{lat}/{lon}/{nm}`) | ODbL。**短時間に続けて叩くと空の応答が返る**。間隔を空ける |
| adsb.fi API(突き合わせ用) | `https://opendata.adsb.fi/api/v2/lat/{lat}/lon/{lon}/dist/{nm}` | **個人の非商用利用のみ**、出典明記。1秒1回まで。公開物には混ぜない |
| adsbdb(機体の名簿) | `https://api.adsbdb.com/v0/aircraft/{hex}` | 登録記号・機種・所有者。位置は持たない |

### adsb.lol アーカイブの形

- 1日分は**約3.4GB**(`.tar.aa` と `.tar.ab` の2分割、全世界)。`cat *.aa *.ab | tar -xf -` で展開
- prod と staging がある。通常は prod、片方が極端に小さい日はもう片方を使う。MLAT のみの版もある
- 中身は **readsb の `/var/globe_history` そのもの**(形式は wiedehopf/readsb の README-json.md「trace jsons」)
  - `traces/`:機体ごとの1日の航跡(gzip 圧縮 JSON)
  - `heatmap/`:30分ごとの全機体の位置スナップショット(タイルごとの密度を数えるのに使える)
- JSON のフィールド名は kikicom のログ(同じ readsb)と同じ。kikicom の `field-stats.py` や
  `review-aircraft.py` の考え方がそのまま使える

---

## 3. 解析の単位:ズームレベル4のタイル(z/x/y)

ロングリスト(ここから絞る):

| タイル | 経度 | 緯度 | 中身 | 備考 |
|---|---|---|---|---|
| 4/14/5 | 135.0–157.5 | 41.0–55.8 | 北海道、サハリン、千島 | kikicom の受信機で答え合わせできる |
| 4/14/6 | 135.0–157.5 | 21.9–41.0 | 本州東半分、首都圏 | adsb.lol の受信網が最も厚い(東京100nm圏で約85機) |
| 4/13/6 | 112.5–135.0 | 21.9–41.0 | 朝鮮半島、中国東部、台湾、九州 | 韓国は厚い、中国側は薄いと思われる |
| 4/9/4 | 22.5–45.0 | 55.8–66.5 | フィンランド南部、バルト三国 | adsb.fi にしか見えない機体がある(下記) |
| 4/9/6 | 22.5–45.0 | 21.9–41.0 | ギリシャ、トルコ、キプロス、レバント、エジプト | 紛争空域を含む。配慮が特に要る |
| 4/7/7 | -22.5–0.0 | 0.0–21.9 | シエラレオネを含む西アフリカ沿岸 | adsb.lol ではほぼ空(2026-09-22 朝の時点で0機) |

**絞り込みの物差し(案)**:データの厚さ(1日分の機体数・位置点数・空白の広さ)、航空測量との結びつき、
配慮の重さ、答え合わせのしやすさ。最初の一歩は、平日1日分のアーカイブ(9/18 金を想定)で6枚を数えること。

### 受信網についての観察(2026-09-22 朝、同時刻の比較)

- 東京100nm・札幌150nm:adsb.lol と adsb.fi は**完全に一致**(同じ受信局が両方に送っていると見られる)
- ヘルシンキ150nm・タリン120nm:**adsb.fi にしか見えない機体が半数**(現地は深夜で標本は3〜6機)。
  4/9/4 を adsb.lol で解析するなら、欠けの程度を昼間に adsb.fi で見積もっておくこと
- 受信網の厚さは地域で大きく違う。**「見えない」は「飛んでいない」ではない**。測量機は地方の低めの高度を
  飛ぶことが多く、受信局の少ない地域では欠けやすい。古い機体には ADS-B の位置を送らないものもある

---

## 4. 配慮の線

- 解析結果は公開しない(ユーザー自身の学習用)
- 航空測量の機体の解析は技術の観点で行う。**特定の事業者の活動を会社別に追跡・蓄積するような使い方はしない**
- 軍用機・要人機・国の機体は、見えても扱いを慎重にする(kikicom の CLAUDE.md の整理を引き継ぐ)。
  4/9/6(紛争空域)、4/14/5(サハリン・千島)、4/13/6(朝鮮半島)は特に
- ローカルの機体名(ホスト名)はリポジトリに書かない。役割で呼ぶ(「集約用の Mac」など)

---

## 5. 動かす環境

- データとツールは、Mac mini 役の集約用 Mac の外付けボリューム上
  (`/Volumes/Migrate-2025-04/github/adsb-survey`、空き数百GB)に置く。Claude Code から ssh で入って動かす
- **macOS の注意(kikicom で踏んだもの)**
  - launchd の定期ジョブは、外付けボリューム上のスクリプトを実行できない(TCC、"Operation not permitted")。
    定期実行するなら `~/.local/lib/...` にコピーして実行する
  - launchd 配下で Homebrew の(フレームワーク版)python3 から起動した ssh は、ローカルネットワークの
    プライバシー保護で "No route to host" になる。LAN 内の機体に ssh するジョブは bash を入口にする
- 1日分のアーカイブを取得する前に、空き容量と展開後の容量を確認すること

---

## 6. ステータス

- [x] リポジトリ作成(public、CC0、`data/` を gitignore、README の骨組み)
- [ ] 1日分のアーカイブの取得と展開(9/18 金・9/16 水の2日分。経緯は
  [DECISIONS.md#d2](DECISIONS.md#d2-初期調査対象日の決定9189/16の比較祝日確認) 参照)
- [ ] ロングリスト6枚の「データの厚さ」の計数と絞り込み
- [ ] 航空測量機の見つけ方(機種・登録・adsbdb の所有者)と、撮影測線の検出の試作
